<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neighborhood.mapper.ServiceMapper">
    <select id="selectServiceList" resultType="com.neighborhood.entity.ServiceInfo">
        SELECT s.*, m.name as merchant_name, c.name as category_name 
        FROM service s 
        LEFT JOIN merchant m ON s.merchant_id = m.id 
        LEFT JOIN service_category c ON s.category_id = c.id 
        WHERE s.deleted = 0 AND s.status = 1
    </select>
</mapper>
